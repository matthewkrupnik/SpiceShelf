# Agents

## Build

```bash
xcodebuild -project SpiceShelf.xcodeproj -scheme SpiceShelf -destination 'platform=iOS Simulator,name=iPhone 17 Pro' build
```

## Rules

- Never build or run tests unless explicitly prompted to.
